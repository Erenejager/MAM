---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.cjs
  - frontend/postcss.config.cjs
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `npm run dev` in the frontend directory starts Vite dev server without errors"
    - "The browser shows a page with Cinema Dark background (#0F0F23) and text (#F8FAFC)"
    - "Tailwind utility classes are applied correctly (not raw CSS)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Frontend dependencies — React 18, Vite, TypeScript, Tailwind 3"
      contains: "tailwindcss"
    - path: "frontend/vite.config.ts"
      provides: "Vite build configuration with React plugin"
      contains: "@vitejs/plugin-react"
    - path: "frontend/tailwind.config.cjs"
      provides: "Tailwind 3 config with Cinema Dark design tokens"
      contains: "#0F0F23"
    - path: "frontend/src/index.css"
      provides: "Global styles with Tailwind directives and CSS custom properties"
      contains: "@tailwind base"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "frontend/index.html"
      via: "Vite entry point"
      pattern: "index\\.html"
    - from: "frontend/tailwind.config.cjs"
      to: "frontend/src/index.css"
      via: "Tailwind content scanning"
      pattern: "content.*src"
---

<objective>
Scaffold the Vite + React 18 + TypeScript frontend with Tailwind CSS 3 configured using Cinema Dark design tokens.

Purpose: Create the frontend project structure so it can be developed and built independently. All design system tokens (colors, fonts, spacing, shadows) are wired into Tailwind config from day one.
Output: A working Vite dev server rendering a minimal React app with Cinema Dark styling.
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/mam/MASTER.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React 18 + TypeScript project</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/tsconfig.node.json, frontend/vite.config.ts, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/vite-env.d.ts</files>
  <read_first>design-system/mam/MASTER.md (for layout structure and design tokens), .planning/research/STACK.md (for version decisions)</read_first>
  <action>
Create the frontend directory at the project root. Initialize with:

1. Run `npm create vite@latest frontend -- --template react-ts` OR manually create the files (whichever is cleaner).

2. `package.json` must include these dependencies:
   - `react`: `^18.3.0` (NOT React 19 — per STACK.md)
   - `react-dom`: `^18.3.0`
   - devDependencies: `vite` (^5), `@vitejs/plugin-react`, `typescript` (^5), `@types/react`, `@types/react-dom`

3. `vite.config.ts`:
   ```ts
   import { defineConfig } from 'vite';
   import react from '@vitejs/plugin-react';
   export default defineConfig({
     plugins: [react()],
     server: { port: 5173 },
     build: { outDir: 'dist' }
   });
   ```

4. `tsconfig.json`: target ES2020, module ESNext, jsx react-jsx, strict true, moduleResolution bundler.

5. `index.html`: Standard Vite HTML entry. Include Google Fonts link for Fira Code + Fira Sans:
   `https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600;700&family=Fira+Sans:wght@300;400;500;600;700&display=swap`

6. `src/App.tsx`: Minimal component rendering `<div className="min-h-screen bg-background text-text">MAM</div>` — proves Tailwind works with custom tokens.

7. `src/main.tsx`: Standard React 18 createRoot entry importing `./index.css`.

8. Add `scripts` to package.json: `"dev": "vite"`, `"build": "vite build"`, `"preview": "vite preview"`.

9. Run `npm install` in the frontend directory to generate package-lock.json.
  </action>
  <verify>
    <automated>cd /home/clawdbot/MAM/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Vite builds without errors. dist/ directory created with index.html and JS bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS 3 with Cinema Dark design tokens</name>
  <files>frontend/tailwind.config.cjs, frontend/postcss.config.cjs, frontend/src/index.css</files>
  <read_first>design-system/mam/MASTER.md (color palette, typography, spacing, shadows), frontend/package.json (verify it exists from Task 1)</read_first>
  <action>
1. Install Tailwind 3 + dependencies in frontend:
   `npm install -D tailwindcss@3 postcss autoprefixer`

2. Create `postcss.config.cjs`:
   ```js
   module.exports = { plugins: { tailwindcss: {}, autoprefixer: {} } };
   ```

3. Create `tailwind.config.cjs` (NOT .ts, NOT tailwind.config.mjs — use CJS for Tailwind 3 compatibility):
   ```js
   /** @type {import('tailwindcss').Config} */
   module.exports = {
     content: ['./index.html', './src/**/*.{ts,tsx}'],
     theme: {
       extend: {
         colors: {
           background: '#0F0F23',
           panel: '#1E1B4B',
           cta: '#E11D48',
           'cta-hover': '#BE123C',
           text: '#F8FAFC',
           'text-muted': '#94A3B8',
           border: '#2D2A5E',
           'border-hover': '#4C4891',
           'status-pending': '#94A3B8',
           'status-processing': '#F59E0B',
           'status-complete': '#10B981',
           'status-failed': '#E11D48',
         },
         fontFamily: {
           sans: ['Fira Sans', 'sans-serif'],
           mono: ['Fira Code', 'monospace'],
         },
         spacing: {
           xs: '4px',
           sm: '8px',
           md: '16px',
           lg: '24px',
           xl: '32px',
           '2xl': '48px',
           '3xl': '64px',
         },
         boxShadow: {
           sm: '0 1px 3px rgba(0,0,0,0.4)',
           md: '0 4px 12px rgba(0,0,0,0.5)',
           lg: '0 8px 24px rgba(0,0,0,0.6)',
           accent: '0 0 12px rgba(225,29,72,0.3)',
         },
         borderRadius: {
           DEFAULT: '8px',
           lg: '12px',
           xl: '16px',
         },
       },
     },
     plugins: [],
   };
   ```

4. Create `src/index.css`:
   ```css
   @tailwind base;
   @tailwind components;
   @tailwind utilities;

   :root {
     --color-background: #0F0F23;
     --color-panel: #1E1B4B;
     --color-cta: #E11D48;
     --color-text: #F8FAFC;
     --color-text-muted: #94A3B8;
     --color-border: #2D2A5E;
   }

   body {
     margin: 0;
     background-color: #0F0F23;
     color: #F8FAFC;
     font-family: 'Fira Sans', sans-serif;
   }
   ```

5. Verify Tailwind is processing by confirming `npm run build` produces CSS with the custom color values in the output bundle.
  </action>
  <verify>
    <automated>cd /home/clawdbot/MAM/frontend && npm run build 2>&1 | tail -3 && grep -r "0F0F23" dist/assets/*.css | head -1</automated>
  </verify>
  <done>Tailwind 3 processes utility classes. Built CSS contains Cinema Dark color values (#0F0F23). Custom font families, spacing tokens, and shadow tokens are available as Tailwind utilities.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run dev` starts without errors on port 5173
- `cd frontend && npm run build` produces dist/ with index.html and CSS containing #0F0F23
- tailwind.config.cjs contains all 6 Cinema Dark color tokens
- Google Fonts link present in index.html for Fira Code + Fira Sans
</verification>

<success_criteria>
- Frontend builds and serves without errors
- Tailwind 3 is configured with all Cinema Dark design tokens (colors, fonts, spacing, shadows)
- App renders with dark background (#0F0F23) and light text (#F8FAFC)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
