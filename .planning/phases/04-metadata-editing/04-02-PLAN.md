---
phase: 04-metadata-editing
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/types/asset.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useAssets.ts
  - frontend/src/components/detail/InlineEditText.tsx
  - frontend/src/components/detail/InlineEditTextarea.tsx
  - frontend/src/components/detail/TagEditor.tsx
autonomous: true
requirements: [META-02, META-03, META-04]

must_haves:
  truths:
    - "InlineEditText renders as plain clickable text in read mode and an input in edit mode"
    - "InlineEditText saves on blur or Enter, cancels on Escape (with stopPropagation to prevent detail panel close)"
    - "InlineEditText flashes green border on success and red border on error for 800ms"
    - "InlineEditTextarea auto-expands as the user types (scrollHeight technique)"
    - "InlineEditTextarea saves on Enter, inserts newline on Shift+Enter"
    - "TagEditor renders existing tags as chips with remove buttons, plus a + button to add"
    - "TagEditor autocomplete filters from cached /api/tags data client-side (150ms debounce)"
    - "TagEditor shows Create 'x' option when typed value is not in existing tags"
    - "All new API functions in api.ts call the correct endpoints with correct HTTP methods"
    - "usePatchAsset hook uses onMutate/onError/onSettled for optimistic updates with rollback"
  artifacts:
    - path: "frontend/src/components/detail/InlineEditText.tsx"
      provides: "Click-to-edit single-line text component"
      exports: ["InlineEditText"]
    - path: "frontend/src/components/detail/InlineEditTextarea.tsx"
      provides: "Click-to-edit auto-expanding textarea component"
      exports: ["InlineEditTextarea"]
    - path: "frontend/src/components/detail/TagEditor.tsx"
      provides: "Tag chip row with autocomplete dropdown"
      exports: ["TagEditor"]
    - path: "frontend/src/types/asset.ts"
      provides: "CustomField, CustomValue interfaces"
      contains: "CustomField"
    - path: "frontend/src/hooks/useAssets.ts"
      provides: "usePatchAsset, useCustomFields, useCreateCustomField, useDeleteCustomField, useCustomValues, usePatchCustomValue hooks"
      exports: ["usePatchAsset"]
  key_links:
    - from: "InlineEditText/InlineEditTextarea"
      to: "onSave prop"
      via: "onSave(draft) called on blur or Enter"
      pattern: "onSave\\(draft\\)"
    - from: "TagEditor"
      to: "useTags() cache"
      via: "client-side filter of tags query data"
      pattern: "useQuery.*tags"
    - from: "usePatchAsset"
      to: "queryClient cache"
      via: "onMutate snapshot + onError rollback"
      pattern: "onMutate.*cancelQueries"
---

<objective>
Build all frontend primitive components and hooks for metadata editing.

Purpose: These are the reusable building blocks that Plan 03 wires into the actual UI. Establishing InlineEditText/InlineEditTextarea/TagEditor as clean primitives ensures consistent behaviour across title, description, and custom field values.

Output:
- InlineEditText (click-to-edit single line, border flash, Escape stopPropagation)
- InlineEditTextarea (auto-expand, Shift+Enter newline, same flash feedback)
- TagEditor (chip row + autocomplete dropdown with Create option)
- New API functions in api.ts (patchAsset, fetchCustomFields, createCustomField, deleteCustomField, patchCustomValue, fetchCustomValues)
- New types in asset.ts (CustomField, CustomValue)
- New hooks in useAssets.ts (usePatchAsset, useCustomFields, useCreateCustomField, useDeleteCustomField, useCustomValues, usePatchCustomValue)
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-metadata-editing/04-CONTEXT.md
@.planning/phases/04-metadata-editing/04-UI-SPEC.md
@.planning/phases/04-metadata-editing/04-RESEARCH.md
@.planning/phases/04-metadata-editing/04-01-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor must replicate and extend. -->

From frontend/src/types/asset.ts (current — extend, do not replace):
```typescript
export interface Asset {
  id: string;
  originalFilename: string;
  title: string | null;
  description: string | null;
  tags: string; // JSON string — parse with JSON.parse() to get string[]
  // ... (all other existing fields remain)
}
// TagCount already exists

// ADD these new interfaces:
export interface CustomField {
  id: string;
  name: string;
  fieldType: string; // 'text' in Phase 4
  createdAt: string;
}

export interface CustomValue {
  assetId: string;
  fieldId: string;
  value: string | null;
}
```

From frontend/src/lib/api.ts (current exports — ADD these, do not replace existing):
```typescript
// ADD:
export async function patchAsset(id: string, data: { title?: string; description?: string }): Promise<Asset>
export async function fetchCustomFields(): Promise<CustomField[]>
export async function createCustomField(name: string): Promise<CustomField>
export async function deleteCustomField(id: string): Promise<void>
export async function fetchCustomValues(assetId: string): Promise<CustomValue[]>
export async function patchCustomValue(assetId: string, fieldId: string, value: string): Promise<CustomValue>
```

From frontend/src/hooks/useAssets.ts (current exports — ADD these):
```typescript
// ADD (do not replace existing hooks):
export function usePatchAsset()   // TanStack Query useMutation with onMutate/onError/onSettled
export function useCustomFields() // useQuery(['custom-fields'])
export function useCreateCustomField()  // useMutation → invalidate ['custom-fields']
export function useDeleteCustomField()  // useMutation → invalidate ['custom-fields']
export function useCustomValues(assetId: string)  // useQuery(['custom-values', assetId])
export function usePatchCustomValue()  // useMutation → invalidate ['custom-values', assetId]
```

From frontend/src/lib/cn.ts (already exists):
```typescript
export function cn(...classes: (string | undefined | null | false)[]): string
// Use for conditional class merging in all new components
```

Design token classes (from tailwind.config.cjs, already defined):
- bg-background (#0F0F23), bg-panel (#1E1B4B), bg-cta (#E11D48)
- text-text (#F8FAFC), text-text-muted (#94A3B8), text-cta
- border-border (#2D2A5E), border-cta
- border-status-complete (#10B981), border-status-failed (#E11D48)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, API functions, and hooks</name>
  <files>
    frontend/src/types/asset.ts
    frontend/src/lib/api.ts
    frontend/src/hooks/useAssets.ts
  </files>
  <read_first>
    - frontend/src/types/asset.ts (full file — append CustomField and CustomValue interfaces)
    - frontend/src/lib/api.ts (full file — append new functions, preserve all existing)
    - frontend/src/hooks/useAssets.ts (full file — append new hooks, preserve all existing)
  </read_first>
  <action>
    **asset.ts** — append after the TagCount interface:
    ```typescript
    export interface CustomField {
      id: string;
      name: string;
      fieldType: string;
      createdAt: string;
    }

    export interface CustomValue {
      assetId: string;
      fieldId: string;
      value: string | null;
    }
    ```

    **api.ts** — append after patchAssetTags:
    ```typescript
    import type { CustomField, CustomValue } from '../types/asset';

    export async function patchAsset(id: string, data: { title?: string; description?: string }): Promise<Asset> {
      const res = await fetch(`${API_BASE}/assets/${id}`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });
      if (!res.ok) throw new Error(`Failed to update asset: ${res.status}`);
      return res.json();
    }

    export async function fetchCustomFields(): Promise<CustomField[]> {
      const res = await fetch(`${API_BASE}/custom-fields`);
      if (!res.ok) throw new Error(`Failed to fetch custom fields: ${res.status}`);
      return res.json();
    }

    export async function createCustomField(name: string): Promise<CustomField> {
      const res = await fetch(`${API_BASE}/custom-fields`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name, type: 'text' }),
      });
      if (!res.ok) {
        const body = await res.json().catch(() => ({}));
        throw new Error((body as { error?: string }).error ?? `Failed to create custom field: ${res.status}`);
      }
      return res.json();
    }

    export async function deleteCustomField(id: string): Promise<void> {
      const res = await fetch(`${API_BASE}/custom-fields/${id}`, { method: 'DELETE' });
      if (!res.ok) throw new Error(`Failed to delete custom field: ${res.status}`);
    }

    export async function fetchCustomValues(assetId: string): Promise<CustomValue[]> {
      const res = await fetch(`${API_BASE}/assets/${assetId}/custom-values`);
      if (!res.ok) throw new Error(`Failed to fetch custom values: ${res.status}`);
      return res.json();
    }

    export async function patchCustomValue(assetId: string, fieldId: string, value: string): Promise<CustomValue> {
      const res = await fetch(`${API_BASE}/assets/${assetId}/custom-values/${fieldId}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ value }),
      });
      if (!res.ok) throw new Error(`Failed to update custom value: ${res.status}`);
      return res.json();
    }
    ```

    **useAssets.ts** — update import line to include new API functions, then append new hooks:
    ```typescript
    import {
      fetchAssets, fetchAsset, fetchTags, deleteAsset, patchAssetTags,
      patchAsset, fetchCustomFields, createCustomField, deleteCustomField,
      fetchCustomValues, patchCustomValue,
    } from '../lib/api';
    import type { Asset, CustomField, CustomValue } from '../types/asset';

    export function usePatchAsset() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: ({ id, data }: { id: string; data: { title?: string; description?: string } }) =>
          patchAsset(id, data),
        onMutate: async ({ id, data }) => {
          await queryClient.cancelQueries({ queryKey: ['assets', id] });
          const previous = queryClient.getQueryData<Asset>(['assets', id]);
          queryClient.setQueryData<Asset>(['assets', id], (old) => old ? { ...old, ...data } : old);
          return { previous };
        },
        onError: (_err, { id }, context) => {
          if (context?.previous) {
            queryClient.setQueryData(['assets', id], context.previous);
          }
        },
        onSettled: (_data, _err, { id }) => {
          queryClient.invalidateQueries({ queryKey: ['assets', id] });
          queryClient.invalidateQueries({ queryKey: ['assets'] });
        },
      });
    }

    export function useCustomFields() {
      return useQuery<CustomField[]>({
        queryKey: ['custom-fields'],
        queryFn: fetchCustomFields,
      });
    }

    export function useCreateCustomField() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: (name: string) => createCustomField(name),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['custom-fields'] });
        },
      });
    }

    export function useDeleteCustomField() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: (id: string) => deleteCustomField(id),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['custom-fields'] });
        },
      });
    }

    export function useCustomValues(assetId: string) {
      return useQuery<CustomValue[]>({
        queryKey: ['custom-values', assetId],
        queryFn: () => fetchCustomValues(assetId),
        enabled: !!assetId,
      });
    }

    export function usePatchCustomValue() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: ({ assetId, fieldId, value }: { assetId: string; fieldId: string; value: string }) =>
          patchCustomValue(assetId, fieldId, value),
        onSuccess: (_data, { assetId }) => {
          queryClient.invalidateQueries({ queryKey: ['custom-values', assetId] });
        },
      });
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "CustomField" /home/clawdbot/MAM/frontend/src/types/asset.ts` shows the CustomField interface exported
    - `grep -n "CustomValue" /home/clawdbot/MAM/frontend/src/types/asset.ts` shows the CustomValue interface exported
    - `grep -n "patchAsset" /home/clawdbot/MAM/frontend/src/lib/api.ts` shows the function exported
    - `grep -n "fetchCustomFields\|createCustomField\|deleteCustomField\|patchCustomValue\|fetchCustomValues" /home/clawdbot/MAM/frontend/src/lib/api.ts` shows all 5 functions
    - `grep -n "usePatchAsset\|useCustomFields\|useCreateCustomField\|useDeleteCustomField\|useCustomValues\|usePatchCustomValue" /home/clawdbot/MAM/frontend/src/hooks/useAssets.ts` shows all 6 hooks
    - `grep -n "onMutate" /home/clawdbot/MAM/frontend/src/hooks/useAssets.ts` shows optimistic update pattern in usePatchAsset
    - `grep -n "cancelQueries" /home/clawdbot/MAM/frontend/src/hooks/useAssets.ts` confirms race condition prevention
    - `cd /home/clawdbot/MAM/frontend && npx tsc --noEmit 2>&1 | head -5` shows no errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/clawdbot/MAM/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compilation is clean (0 errors). All 6 new hooks and 6 new API functions are present. CustomField and CustomValue interfaces are exported from types/asset.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: InlineEditText and InlineEditTextarea components</name>
  <files>
    frontend/src/components/detail/InlineEditText.tsx
    frontend/src/components/detail/InlineEditTextarea.tsx
  </files>
  <read_first>
    - frontend/src/lib/cn.ts (cn() function signature for class merging)
    - frontend/src/components/detail/MetadataSection.tsx (understand context where these will be used)
    - .planning/phases/04-metadata-editing/04-UI-SPEC.md (InlineEditText and InlineEditTextarea component specs, animation contract)
    - .planning/phases/04-metadata-editing/04-CONTEXT.md (Escape stopPropagation requirement, Shift+Enter for textarea)
  </read_first>
  <action>
    **InlineEditText.tsx** — new file:

    Props interface:
    ```typescript
    interface InlineEditTextProps {
      value: string | null;
      onSave: (newValue: string) => Promise<void>;
      placeholder?: string;     // shown when value is null/empty in read mode
      ariaLabel?: string;
      className?: string;       // passed to read-mode span for layout flexibility
    }
    ```

    State: `isEditing: boolean`, `draft: string`, `isSaving: boolean`, `flashState: 'idle' | 'success' | 'error'`

    Read mode (isEditing === false):
    ```tsx
    <span
      role="button"
      aria-label={ariaLabel ?? 'Edit'}
      onClick={() => { setDraft(value ?? ''); setIsEditing(true); }}
      className={cn(
        'cursor-pointer text-sm text-text hover:underline decoration-dashed decoration-text-muted underline-offset-2',
        !value && 'text-text-muted italic',
        className
      )}
    >
      {value || placeholder || '—'}
    </span>
    ```

    Edit mode (isEditing === true):
    ```tsx
    <input
      ref={inputRef}
      type="text"
      value={draft}
      onChange={e => setDraft(e.target.value)}
      onBlur={handleSave}
      onKeyDown={handleKeyDown}
      disabled={isSaving}
      aria-label={ariaLabel ?? 'Edit'}
      className={cn(
        'w-full bg-background rounded px-2 py-1 text-sm text-text font-sans',
        'border outline-none transition-colors duration-200',
        'focus:shadow-[0_0_0_3px_rgba(225,29,72,0.15)]',
        isSaving && 'opacity-70',
        flashState === 'idle' && 'border-border focus:border-cta',
        flashState === 'success' && 'border-status-complete',
        flashState === 'error' && 'border-status-failed',
      )}
    />
    ```

    handleKeyDown:
    ```typescript
    const handleKeyDown = (e: React.KeyboardEvent) => {
      if (e.key === 'Enter') { e.preventDefault(); handleSave(); }
      if (e.key === 'Escape') {
        e.stopPropagation(); // CRITICAL: prevent DetailPanel Escape listener from firing
        setDraft(value ?? '');
        setIsEditing(false);
      }
    };
    ```

    handleSave (async):
    ```typescript
    const handleSave = async () => {
      if (draft === (value ?? '')) { setIsEditing(false); return; }
      setIsSaving(true);
      setIsEditing(false);
      try {
        await onSave(draft);
        setFlashState('success');
      } catch {
        setDraft(value ?? '');
        setFlashState('error');
      } finally {
        setIsSaving(false);
        setTimeout(() => setFlashState('idle'), 800);
      }
    };
    ```

    useEffect to focus + select when entering edit mode:
    ```typescript
    useEffect(() => {
      if (isEditing && inputRef.current) {
        inputRef.current.focus();
        inputRef.current.select();
      }
    }, [isEditing]);
    ```

    Reduced motion: Use CSS `@media (prefers-reduced-motion: reduce)` to disable transitions. Do NOT import `useReducedMotion` from framer-motion — it only affects Framer Motion animations, not CSS transitions. The 800ms setTimeout for flash reset is fine regardless of motion preference.

    **InlineEditTextarea.tsx** — same structure as InlineEditText with these differences:
    - Renders `<textarea>` instead of `<input>` in edit mode
    - `rows={1}` always (height driven by scrollHeight)
    - Add `resize-none overflow-hidden` classes
    - onInput handler for auto-expand:
      ```typescript
      const handleInput = (e: React.FormEvent<HTMLTextAreaElement>) => {
        const el = e.currentTarget;
        el.style.height = 'auto';
        el.style.height = `${el.scrollHeight}px`;
      };
      ```
    - handleKeyDown: Shift+Enter inserts newline (do NOT call handleSave); bare Enter saves
      ```typescript
      if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); handleSave(); }
      if (e.key === 'Enter' && e.shiftKey) { /* allow default newline */ return; }
      if (e.key === 'Escape') { e.stopPropagation(); setDraft(value ?? ''); setIsEditing(false); }
      ```
    - Read mode placeholder text is "No description" (passed via placeholder prop)
    - useEffect for focus: also run `handleInput` synthetic call to set initial height
  </action>
  <acceptance_criteria>
    - `grep -n "stopPropagation" /home/clawdbot/MAM/frontend/src/components/detail/InlineEditText.tsx` shows e.stopPropagation() on Escape
    - `grep -n "stopPropagation" /home/clawdbot/MAM/frontend/src/components/detail/InlineEditTextarea.tsx` shows e.stopPropagation() on Escape
    - `grep -n "flashState" /home/clawdbot/MAM/frontend/src/components/detail/InlineEditText.tsx` shows 'idle' | 'success' | 'error' states
    - `grep -n "scrollHeight" /home/clawdbot/MAM/frontend/src/components/detail/InlineEditTextarea.tsx` shows auto-expand logic
    - `grep -n "shiftKey" /home/clawdbot/MAM/frontend/src/components/detail/InlineEditTextarea.tsx` shows Shift+Enter guard
    - `grep -n "border-status-complete\|border-status-failed" /home/clawdbot/MAM/frontend/src/components/detail/InlineEditText.tsx` shows flash class names
    - `grep -n "useReducedMotion" /home/clawdbot/MAM/frontend/src/components/detail/InlineEditText.tsx` shows reduced motion support
    - `cd /home/clawdbot/MAM/frontend && npx tsc --noEmit 2>&1 | head -5` shows 0 errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/clawdbot/MAM/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compilation is clean. Both components exist. InlineEditText and InlineEditTextarea handle Escape with stopPropagation, show border flash on save, and auto-expand (textarea) on input.
  </done>
</task>

<task type="auto">
  <name>Task 3: TagEditor component</name>
  <files>
    frontend/src/components/detail/TagEditor.tsx
  </files>
  <read_first>
    - frontend/src/hooks/useAssets.ts (useTags hook — used to get existing tags for autocomplete)
    - frontend/src/types/asset.ts (TagCount interface)
    - frontend/src/lib/cn.ts (cn() function)
    - .planning/phases/04-metadata-editing/04-UI-SPEC.md (TagEditor component spec, autocomplete dropdown spec, keyboard nav spec)
    - .planning/phases/04-metadata-editing/04-UI-SPEC.md (Interaction Contracts — Tag Add Flow and Tag Remove Flow)
  </read_first>
  <action>
    **TagEditor.tsx** — new file.

    Props:
    ```typescript
    interface TagEditorProps {
      tags: string[];           // current tags array (already parsed from JSON)
      onTagsChange: (newTags: string[]) => Promise<void>; // called with full updated array
    }
    ```

    Internal state:
    ```typescript
    const [isOpen, setIsOpen] = useState(false);         // dropdown open
    const [inputValue, setInputValue] = useState('');    // typed value
    const [activeIndex, setActiveIndex] = useState(-1);  // keyboard nav
    const [localTags, setLocalTags] = useState(tags);   // optimistic local copy
    ```

    Sync localTags when tags prop changes (useEffect on tags).

    **Tag chips + add button render:**
    ```tsx
    <div className="flex flex-wrap items-center gap-1.5">
      {localTags.map(tag => (
        <span key={tag} className="inline-flex items-center gap-1 bg-panel/80 border border-border text-text-muted px-2 py-0.5 rounded text-xs font-mono cursor-default">
          {tag}
          <button
            onClick={() => handleRemoveTag(tag)}
            aria-label={`Remove tag ${tag}`}
            className="w-3.5 h-3.5 text-text-muted hover:text-cta cursor-pointer transition-colors duration-150 flex items-center justify-center"
          >
            <X className="w-3 h-3" />
          </button>
        </span>
      ))}
      <div className="relative" ref={dropdownRef}>
        <button
          onClick={() => setIsOpen(true)}
          aria-label="Add tag"
          className="w-6 h-6 rounded border border-border text-text-muted hover:border-cta hover:text-cta cursor-pointer transition-colors duration-150 flex items-center justify-center"
        >
          <Plus className="w-3.5 h-3.5" />
        </button>
        {isOpen && <TagAutocomplete ... />}
      </div>
    </div>
    ```

    **TagAutocomplete** (inline subcomponent or separate section within TagEditor.tsx):
    - Get existing tags: `const { data: tagCounts } = useTags();`
    - Compute filtered options with 150ms debounce:
      ```typescript
      const [debouncedInput, setDebouncedInput] = useState('');
      useEffect(() => {
        const t = setTimeout(() => setDebouncedInput(inputValue), 150);
        return () => clearTimeout(t);
      }, [inputValue]);

      const existingTagNames = tagCounts?.map(tc => tc.tag) ?? [];
      const filteredTags = debouncedInput
        ? existingTagNames.filter(t => t.toLowerCase().includes(debouncedInput.toLowerCase()) && !localTags.includes(t))
        : existingTagNames.filter(t => !localTags.includes(t));
      const showCreate = debouncedInput && !existingTagNames.includes(debouncedInput) && !localTags.includes(debouncedInput);
      ```

    - Dropdown container:
      ```tsx
      <div
        role="listbox"
        className="absolute z-50 mt-1 w-56 bg-panel border border-border rounded-lg shadow-lg overflow-hidden"
      >
        <input
          ref={inputRef}
          autoFocus
          role="combobox"
          aria-label="Search tags"
          placeholder="Type to search..."
          value={inputValue}
          onChange={e => { setInputValue(e.target.value); setActiveIndex(-1); }}
          onKeyDown={handleDropdownKeyDown}
          className="w-full bg-background border-b border-border px-3 py-2 text-sm text-text placeholder:text-text-muted outline-none"
        />
        <div className="max-h-[228px] overflow-y-auto">
          {filteredTags.length === 0 && !showCreate && (
            <p className="px-3 py-2 text-sm text-text-muted italic">No matching tags</p>
          )}
          {filteredTags.map((tag, i) => (
            <button
              key={tag}
              role="option"
              id={`tag-option-${i}`}
              aria-selected={activeIndex === i}
              onClick={() => handleSelectTag(tag)}
              className={cn(
                'w-full text-left px-3 py-1.5 text-sm text-text cursor-pointer transition-colors duration-150',
                activeIndex === i ? 'bg-background' : 'hover:bg-background'
              )}
            >
              {tag}
            </button>
          ))}
          {showCreate && (
            <button
              role="option"
              id="tag-option-create"
              aria-selected={activeIndex === filteredTags.length}
              onClick={() => handleSelectTag(debouncedInput)}
              className={cn(
                'w-full text-left px-3 py-1.5 text-sm text-cta cursor-pointer transition-colors duration-150',
                activeIndex === filteredTags.length ? 'bg-background' : 'hover:bg-background'
              )}
            >
              Create &quot;{debouncedInput}&quot;
            </button>
          )}
        </div>
      </div>
      ```

    - handleDropdownKeyDown:
      ```typescript
      const totalOptions = filteredTags.length + (showCreate ? 1 : 0);
      if (e.key === 'ArrowDown') { e.preventDefault(); setActiveIndex(i => Math.min(i + 1, totalOptions - 1)); }
      if (e.key === 'ArrowUp') { e.preventDefault(); setActiveIndex(i => Math.max(i - 1, -1)); }
      if (e.key === 'Enter') {
        e.preventDefault();
        if (activeIndex >= 0 && activeIndex < filteredTags.length) handleSelectTag(filteredTags[activeIndex]);
        else if (activeIndex === filteredTags.length && showCreate) handleSelectTag(debouncedInput);
        else if (debouncedInput) handleSelectTag(debouncedInput);
      }
      if (e.key === 'Escape') { e.stopPropagation(); setIsOpen(false); setInputValue(''); }
      ```

    - handleSelectTag(tag: string):
      ```typescript
      const newTags = [...localTags, tag];
      setLocalTags(newTags);      // optimistic
      setIsOpen(false);
      setInputValue('');
      setActiveIndex(-1);
      onTagsChange(newTags).catch(() => setLocalTags(tags)); // revert on error
      ```

    - handleRemoveTag(tag: string):
      ```typescript
      const newTags = localTags.filter(t => t !== tag);
      setLocalTags(newTags);      // optimistic
      onTagsChange(newTags).catch(() => setLocalTags(tags)); // revert on error
      ```

    - Click-outside dismiss: `useEffect` that adds `mousedown` listener on `document`, checks if `dropdownRef.current?.contains(event.target)`, closes if outside.

    - Imports: `Plus`, `X` from 'lucide-react'; `useTags` from '../../hooks/useAssets'; `cn` from '../../lib/cn'; `useReducedMotion` from 'framer-motion' (apply to AnimatePresence if used for dropdown, otherwise CSS only is fine).
  </action>
  <acceptance_criteria>
    - `grep -n "Create" /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx` shows Create option rendering
    - `grep -n "stopPropagation" /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx` shows Escape stopPropagation in dropdown
    - `grep -n "handleRemoveTag\|handleSelectTag" /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx` shows both handlers with optimistic + revert pattern
    - `grep -n "onTagsChange\(newTags\)\.catch" /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx` shows revert-on-error for both add and remove
    - `grep -n "ArrowDown\|ArrowUp" /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx` shows keyboard navigation
    - `grep -n "setTimeout.*150" /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx` shows 150ms debounce
    - `grep -n "aria-label.*Remove tag\|Remove tag" /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx` shows accessible remove button
    - `cd /home/clawdbot/MAM/frontend && npx tsc --noEmit 2>&1 | head -5` shows 0 errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/clawdbot/MAM/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compilation is clean. TagEditor.tsx exists with chip row, + button, autocomplete dropdown, keyboard navigation (Arrow Up/Down/Enter/Escape), Create option, optimistic add/remove with revert on error, and 150ms debounce.
  </done>
</task>

</tasks>

<verification>
Frontend TypeScript compiles clean:
`cd /home/clawdbot/MAM/frontend && npx tsc --noEmit`

All 3 new component files exist:
`ls /home/clawdbot/MAM/frontend/src/components/detail/InlineEditText.tsx /home/clawdbot/MAM/frontend/src/components/detail/InlineEditTextarea.tsx /home/clawdbot/MAM/frontend/src/components/detail/TagEditor.tsx`

All 6 new hooks exist in useAssets.ts:
`grep -c "export function use" /home/clawdbot/MAM/frontend/src/hooks/useAssets.ts`
(should be at least 10: original 4 + 6 new)
</verification>

<success_criteria>
- `cd /home/clawdbot/MAM/frontend && npx tsc --noEmit` exits 0
- InlineEditText.tsx: Escape calls e.stopPropagation(), border flash uses border-status-complete/border-status-failed, useReducedMotion imported
- InlineEditTextarea.tsx: Shift+Enter guard present, scrollHeight auto-expand present, stopPropagation on Escape
- TagEditor.tsx: Create option, 150ms debounce, keyboard nav, optimistic revert pattern
- usePatchAsset uses onMutate/onError/onSettled with cancelQueries + snapshot
- All 6 new API functions in api.ts call correct endpoints (PATCH /api/assets/:id, GET/POST/DELETE /api/custom-fields, GET/PUT custom-values)
</success_criteria>

<output>
After completion, create `.planning/phases/04-metadata-editing/04-02-SUMMARY.md` using the summary template at `/home/clawdbot/.claude/get-shit-done/templates/summary.md`.
</output>
